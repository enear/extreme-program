var models = require('./modules/models');
var tests = require('./modules/tests');
var users = require('./modules/users');

module.exports = {
        models: models,
        tests: tests,
        users: users
};
